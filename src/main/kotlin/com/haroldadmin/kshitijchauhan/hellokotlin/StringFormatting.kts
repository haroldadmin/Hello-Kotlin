/**
* String interpolation
*/

// String interpolation allows you to insert values into strings easily
// Eliminates the need for concatenation, and has a more readable syntax
val name = "Kotlin"
val greeting = "Hello, ${name}!"
println(greeting)